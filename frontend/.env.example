# Copy this file to .env for local development (optional — the Vite proxy
# in vite.config.js already forwards /api to localhost:5000 in dev).
# On Render, set VITE_API_URL as an Environment Variable on the Static Site
# to your deployed backend's URL, e.g. https://your-backend.onrender.com

VITE_API_URL=
